/**
 * USDC SPL token mint on Solana mainnet-beta.
 * Confirmed against Circle's own docs (circle.com/multi-chain-usdc/solana),
 * not facilitator-specific — this is the one constant in this file that
 * isn't a [TODO: confirm].
 */
export const USDC_MINT_MAINNET = "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v";

/** USDC has 6 decimals on Solana (same as everywhere else). */
export const USDC_DECIMALS = 6;
